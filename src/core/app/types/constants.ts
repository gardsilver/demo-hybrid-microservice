export const GLOBAL_ROUTE_PREFIX = 'api';

export enum KafkaServers {
  MAIN_KAFKA_BROKER = 'Main Kafka Broker',
  DEFAULT_GROUP_ID = 'default-group',
}

export enum RabbitMqServers {
  MAIN_RABBIT_MQ_BROKER = 'Main RMQ Broker',
}
